type Query {
    authenticate(token:String!, username: String!) : Boolean!
}

type Session{
    Username: String!,
    Token: String!
}

type Mutation {
    createSession(username: String!, pass: String!): Session!
}