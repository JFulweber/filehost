    type Query {
        users: [User]!
        user(UserID:String!): User
    }

    type User {
        _id: ID!
        username: String,
        email: String,
        password: String,
        creationDate: Int
    }

    input UserInput {
        username: String!,
        email: String!,
        password: String!
    }

    type Mutation {
        register(new_user:UserInput!): Boolean!
    }